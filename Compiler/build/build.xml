<project name="Compiler" default="all" basedir="../">
	<description>
		simple example build file
	</description>

	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="tests" location="tests" />
	<property name="bin" location="bin" />
	<property name="target" location="target" />


	<path id="classpath.base">
		<pathelement location="lib/commons-cli-1.2.jar" />
	</path>


	<path id="classpath.test">
		<pathelement location="lib/junit-4.11.jar" />
		<pathelement location="lib/hamcrest-core-1.3.jar" />
		<pathelement location="${bin}" />
		<path refid="classpath.base" />
	</path>



	<target name="build" description="compile the source ">
		<!-- Compile the java code from ${src} into ${bin} -->
		<mkdir dir="${bin}" />
		<javac srcdir="${src}" destdir="${bin}" includeantruntime="false">
			<classpath refid="classpath.base" />
		</javac>
		<echo message="Build done" />
	</target>


	<target name="clean" description="clean up">
		<delete dir="${bin}" />
		<delete dir="${target}" />
	</target>


	<target name="buildJar" depends="clean-build">
		<mkdir dir="${target}" />
		<jar destfile="${target}/${ant.project.name}.jar" filesetmanifest="mergewithoutmain">
			<manifest>
				<attribute name="Main-Class" value="compiler.main.CompilerApp" />
				<attribute name="Class-Path" value="." />
			</manifest>
			<fileset dir="${bin}" />
		</jar>
	</target>


	<target name="clean-build" description="clean and build"
		depends="clean,build" />


	<target name="all" description="clean and build" depends="clean-build,test,buildJar" />


	<target name="buildTests" description="compile test sources"
		depends="build">
		<javac srcdir="${tests}" destdir="${bin}" includeantruntime="false">
			<classpath refid="classpath.test" />
		</javac>
		<echo message="Build of tests done" />
	</target>


	<target name="test" depends="buildTests">
		<junit printsummary="on" fork="true" dir="${basedir}" haltonfailure="yes">
			<classpath>
				<path refid="classpath.test" />
			</classpath>

			<formatter type="brief" usefile="false" />

			<batchtest>
				<fileset dir="${tests}" includes="**/*Test*.java" />
			</batchtest>
		</junit>
	</target>
</project>