language: java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7


env:
  - TARGET=buildJar
  - TARGET=test


script: ant -buildfile ./Compiler/build/build.xml $TARGET

notifications:
  email:
    - compiler@lara.coderworks.de

branches:
  except:
    - /^test.*$/
